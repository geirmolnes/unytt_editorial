[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "unytt-editorial"
version = "0.1.0"
description = "Shared editorial constitution for unytt newsroom tools"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.3"]
